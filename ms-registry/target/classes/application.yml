server:
  port: 8080

spring:
  application:
    name: ms-registry


#配置Eureka Server
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://locathost:8080/eureka/
  server:
    enable-self-preservation: false